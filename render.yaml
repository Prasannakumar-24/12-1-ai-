services:
  - type: web
    name: ai-evolution
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m streamlit run ai.py --server.port $PORT --server.address 0.0.0.0

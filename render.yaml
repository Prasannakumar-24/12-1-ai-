services:
  - type: web
    name: ai-evolution
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run ai.py --server.port $PORT --server.address 0.0.0.0
